apiVersion: apps/v1
kind: Deployment
metadata:
  name: brute-agent
  namespace: brute
  labels:
    app: brute-agent
spec:
  replicas: 1
  selector:
    matchLabels:
      app: brute-agent
  template:
    metadata:
      labels:
        app: brute-agent
    spec:
      containers:
        - name: brute-agent
          image: brute-local:latest
          imagePullPolicy: Never
          ports:
            - containerPort: 9292
              name: http
          env:
            - name: BRUTE_CWD
              value: "/workspace"
            - name: LLM_API_KEY
              valueFrom:
                secretKeyRef:
                  name: brute-secrets
                  key: llm-api-key
                  optional: true
            - name: LLM_PROVIDER
              valueFrom:
                secretKeyRef:
                  name: brute-secrets
                  key: llm-provider
                  optional: true
          livenessProbe:
            httpGet:
              path: /global/health
              port: 9292
            initialDelaySeconds: 10
            periodSeconds: 10
            timeoutSeconds: 5
            failureThreshold: 3
          resources:
            requests:
              memory: "256Mi"
              cpu: "200m"
            limits:
              memory: "1Gi"
              cpu: "1000m"
          volumeMounts:
            - name: workspace
              mountPath: /workspace
      volumes:
        - name: workspace
          emptyDir: {}
---
apiVersion: v1
kind: Service
metadata:
  name: brute-agent
  namespace: brute
spec:
  selector:
    app: brute-agent
  ports:
    - protocol: TCP
      port: 80
      targetPort: 9292
  type: ClusterIP
